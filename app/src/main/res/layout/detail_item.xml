<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="horizontal"
              android:layout_width="match_parent"
              android:layout_height="90dp">
    <ImageView
        android:id="@+id/detail_delete_btn"
        android:layout_width="56dp"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:src="@drawable/ic_created_delete"
        />
    <EditText
        android:id="@+id/detail_tag_textview"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textColor="@color/black"/>

    <TextView
        android:id="@+id/detail_uneditable_text"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:visibility="gone"/>

    <EditText
        android:id="@+id/detail_edit_text"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:visibility="gone"/>

    <Button
        android:id="@+id/detail_copy_btn"
        android:layout_width="wrap_content"
        android:layout_height="38dp"
        android:text="copy"/>

    <Button
        android:id="@+id/detail_paste_btn"
        android:layout_width="wrap_content"
        android:layout_height="38dp"
        android:text="paste"
        android:visibility="gone"/>
</LinearLayout>